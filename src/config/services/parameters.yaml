# config/services/parameters.yaml
# Parámetros globales de la aplicación

# parameters:
#   # Environment
#   env.node_env: '%env(NODE_ENV)%'
#   env.port: '%env(PORT)%'

#   # MongoDB
#   db.mongodb_uri: '%env(MONGODB_URI)%'
#   db.name: 'whatsapp-api'

#   # Application
#   app.name: 'WhatsApp Baileys API'
#   app.version: '1.0.0'

#   # Sessions
#   sessions.path: 'sessions'

#   # Logging
#   log.level: '%env(LOG_LEVEL)%'
#   log.pretty: false

#   # Rate Limiting
#   rate_limit.window_ms: 900000
#   rate_limit.max_requests: 100

services:
  api.config:
    factory:
      class: ../../config/FactoryConfig
      method: 'loadConfig'
    public: true
